name: Update Flake
on:
  schedule:
    - cron: '14 * * * *'
  workflow_dispatch:

concurrency: commit-${{ github.ref }}

jobs:
  update-versions:
    name: Update Flake
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          token: ${{ secrets.COMMIT_PUSH_PAT }}
      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v22
      - name: Set up Nix caching
        uses: me-and/magic-nix-cache-action@restore-only-test
        with:
          save-cache: ${{ github.ref == 'refs/heads/main' }}
          source-url: https://github.com/me-and/magic-nix-cache/releases/download/v0.1.6-unstable-0.1.0/magic-nix-cache-closure-X64-Linux
      - name: Update inputs
        env:
          GIT_AUTHOR_EMAIL: actions@github.com
          GIT_AUTHOR_NAME: GitHub Actions
          GIT_COMMITTER_EMAIL: actions@github.com
          GIT_COMMITTER_NAME: GitHub Actions
        run: nix flake update --commit-lock-file
      - name: Push changes
        run: git push
